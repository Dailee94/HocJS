function largeFactorialMod(n, m) {
    let result = 1
    for (let i = 2; i <= n; i++){
        result = (result * i) % m
    }
    return result
}
let n = Number(prompt())
let m = Number(prompt())
console.log(largeFactorialMod(n,m));
